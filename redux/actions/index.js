export const changeScreen = (task) => ({
  type: "CHANGE_SCREEN",
  payload: task,
});
export const LOGIN = (task) => ({
  type: "LOGIN",
  payload: task,
});
export const LOGOUT = (task) => ({
  type: "LOGOUT",
  payload: task,
});
