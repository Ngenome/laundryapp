export const changeScreen = (task) => ({
  type: "CHANGE_SCREEN",
  payload: task,
});
export const LOGIN = (task) => ({
  type: "LOGIN",
  payload: task,
});
export const LOGOUT = (task) => ({
  type: "LOGOUT",
  payload: task,
});
export const ADDITEM = (task) => ({
  type: "ADDITEM",
  payload: task,
});

export const REMOVEITEM = (task) => ({
  type: "REMOVEITEM",
  payload: task,
});

export const CHANGEITEM = (task) => ({
  type: "CHANGEITEM",
  payload: task,
});
export const ADDLAUNDRYITEM = (task) => ({
  type: "ADDLAUNDRYITEM",
  payload: task,
});

export const REMOVELAUNDRYITEM = (task) => ({
  type: "REMOVELAUNDRYITEM",
  payload: task,
});

export const CHANGELAUNDRYITEM = (task) => ({
  type: "CHANGELAUNDRYITEM",
  payload: task,
});
